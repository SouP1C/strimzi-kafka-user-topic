{{- $KafkaOperator := lookup "apiextensions.k8s.io/v1" "CustomResourceDefinition" "" "kafkas.kafka.strimzi.io" -}}
{{- if $KafkaOperator }}
    {{- range $user := $.Values.global.kafkaUsers }}
        {{- $ctx := merge $user (dict "Values" $.Values) }}
        {{- $rendered := tpl (include "mychart.kafkaUser" $ctx) $ctx }}
        {{- $rendered | indent 0 }}
---
    {{- end }}

{{- end }}
